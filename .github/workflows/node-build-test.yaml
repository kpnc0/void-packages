name: Build nodejs on musl

on:
  push:
    branches:
      - node-compile-test
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-musl-full
      # needed for prepare.sh (to run chroot operations)
      options: --privileged
      volumes:
        - /dev:/dev
    steps:
      # 1. Prepare container (mirrors, deps, non‑root user)
      - name: Prepare container
        run: |
          mkdir -p /etc/xbps.d && cp /usr/share/xbps.d/*-repository-*.conf /etc/xbps.d/
          sed -i 's|repo-default|repo-ci|g' /etc/xbps.d/*-repository-*.conf
          xbps-install -Syu xbps && xbps-install -yu && xbps-install -y sudo bash curl git
          useradd -G xbuilder -M builder

      # 2. Checkout code (works inside container, no Node.js needed)
      - name: Checkout
        uses: classabbyamp/treeless-checkout-action@v1

      # 3. Prepare xbps-src masterdir (as builder)
      - name: Prepare masterdir
        run: |
          chown -R builder:builder .
          sudo -Eu builder common/travis/set_mirror.sh
          sudo -Eu builder common/travis/prepare.sh
          # fetch-xtools is optional; only needed for some advanced features
          common/travis/fetch-xtools.sh

      # 4. Build nodejs (as builder)
      - name: Build nodejs
        run: |
          sudo -Eu builder ./xbps-src pkg nodejs

      - name: Copy artifact to host
        run: |
          mkdir -p /tmp/artifacts
          cp hostdir/binpkgs/nodejs-*.xbps /tmp/artifacts/ 2>/dev/null || true
          ls -la /tmp/artifacts/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nodejs-musl
          path: /tmp/artifacts/nodejs-*.xbps