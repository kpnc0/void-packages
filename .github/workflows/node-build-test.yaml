name: Build nodejs on musl

on:
  push:
    branches:
      - node-compile-test
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install xbps static tools and proot
        run: |
          curl -L -o xbps-static.tar.xz https://repo-default.voidlinux.org/static/xbps-static-latest.x86_64-musl.tar.xz
          sudo tar -xJf xbps-static.tar.xz -C /
          sudo apt-get update
          sudo apt-get install -y proot

      - name: Create masterdir with proper permissions
        run: |
          mkdir -p masterdir-x86_64-musl
          chmod 755 masterdir-x86_64-musl

      - name: Bootstrap using proot
        run: |
          ./xbps-src -m masterdir-x86_64-musl -E proot binary-bootstrap

      - name: Build nodejs using proot
        run: |
          ./xbps-src -m masterdir-x86_64-musl -E proot pkg nodejs

      - name: Upload binary package
        uses: actions/upload-artifact@v4
        with:
          name: nodejs-musl
          path: hostdir/binpkgs/nodejs-*.xbps